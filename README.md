# 道草45 Studio

> **Version 1 / アプリ表示 v0.00.1**<br>
> 思考を書き、寄り道を分類し、その過程を映像として残すためのWindows向け制作ツールです。

![道草45 Studio ロゴ](public/app-logo.png)

## 概要

道草45 Studioは、無限キャンバスへ考えを書き出す「思考モード」と、思考の広がりをスタンプで振り返る「分析モード」を備えたElectronアプリです。

プロジェクトは独自の`.m45`形式で保存できます。キャンバスとマイク音声は録画でき、従来版AviUtlで扱いやすい固定フレームレートのAVIとして保存します。

## Version 1でできること

### 思考モード

- 筆圧対応のペン描画
- 触れた部分だけを消す部分消去
- パン、ズーム、Undo、Redo
- 色とサイズの変更、ユーザー定義プリセット
- 表示・非表示を切り替えられるレイヤー

### キャンバス背景

- 白、黒、紙色
- 無地、ドット、罫線、方眼
- 模様の間隔変更
- パンとズームに追従するワールド座標基準の模様

### 分析モード

- テーマスタンプとユーザー定義スタンプ
- スタンプ名、色、サイズの編集
- スタンプテンプレートの保存と読込
- スタンプ箱のドラッグ移動
- 道草値の表示切替
- スタンプの使用割合を確認する「集計」
- 動画の最後に提示できる「今回のまとめ」
- まとめカードへのペン入力
- まとめカードのPNG出力

道草値は、分類用スタンプのワールド座標全体から、広がり、多方向性、スタンプ数を使って0〜45で算出します。ズーム率やカメラ位置には影響されません。

### 録画とAVI出力

- キャンバスとマイク音声の同時録画
- 720p、1080p、1440p
- 30 fps、60 fps
- 4、8、12、20 Mbps
- 音声入力デバイスの選択
- 録画前のリアルタイムdBFS・ピークレベルモニター
- フィルター適用後音声のヘッドホンモニターとモニター音量調整
- 入力ゲイン（-30〜+30dB）、ハイパス、3バンドEQ
- コンプレッサー、リミッター、OS／ブラウザのノイズ抑制
- 音声レベルと録画時間の表示切替
- 内部収録したWebMを固定30/60fpsへ正規化
- Motion JPEG映像と48kHz PCMステレオ音声を1本にまとめたAVI保存
- 従来版AviUtlの標準入力およびL-SMASH Worksを想定した編集用形式

録画停止後、内部WebMをAVIへ変換してから保存します。変換中はアプリを終了しないでください。AVIは編集互換性を優先しているため、WebMよりファイルサイズが大きくなります。

音声フィルターはOBSの同名設定と同じdB、Hz、比率、ms単位を使用するため、数値を転記できます。ただし、ブラウザ提供のノイズ抑制はOBSのRNNoise／Speexとアルゴリズムが異なり、サイドチェーン・ダッキングにも対応していません。録画前に「レベルモニター」で確認し、通常音声が-18〜-6dBFS、ピークが-3dBFS前後に収まるよう調整してください。

### プロジェクト

- 新規作成、開く、保存、名前を付けて保存
- `.m45`形式へのキャンバス、カメラ、背景、レイヤー、スタンプ設定の保存
- メニューとキーボードショートカットによる操作

## 動作環境

- Windows 11
- Node.js 22系を推奨
- npm
- マイク
- ペンタブレットまたは液晶ペンタブレットを推奨

基準表示は1920×1080です。Windows表示倍率100%および125%を想定しています。

## セットアップ

```powershell
npm install
npm start
```

## テスト

```powershell
npm test
npm exec tsc -- --noEmit
```

## ビルド

アプリをパッケージ化します。

```powershell
npm run package
```

インストーラーと配布用アーカイブを生成します。

```powershell
npm run make
```

## 主なショートカット

| 操作 | ショートカット |
|---|---|
| 新規作成 | `Ctrl+N` |
| 開く | `Ctrl+O` |
| 保存 | `Ctrl+S` |
| Undo | `Ctrl+Z` |
| Redo | `Ctrl+Y` |
| 録画開始・停止 | `R` |
| パン | `Space`を押しながらドラッグ |
| ズーム | マウスホイール |

## プロジェクト構成

```text
assets/                         配布用アイコン、ビルド時に取得するLGPL版FFmpeg
public/
├─ app-logo.png                 アプリ内ロゴ
└─ vendor/EBML.min.js           WebMメタデータ処理
src/
├─ layers/                      レイヤーUI
├─ recording/RecordingManager.ts
├─ review/                      分析、道草値、スタンプ箱
├─ shared/                      プロジェクト形式と共通型
├─ whiteboard/Whiteboard.tsx    キャンバスと主要UI
├─ main.ts                      Electronメインプロセスとファイル保存
└─ preload.ts                   Renderer向けAPI
```

## ライセンスと配布上の注意

Copyright (c) 2026 道草45 (GitHub: Dehara37564). All rights reserved.

このリポジトリはオープンソースとして許諾されていません。利用、変更、再配布の条件は[LICENSE](LICENSE)を確認してください。

AVI変換にはBtbNのLGPL構成FFmpegを独立したコマンドライン実行ファイルとして同梱します。GPL専用部品、libx264、H.264、AAC、MP4変換は使用しません。ビルド時は`npm run prepare:ffmpeg`が固定リリースを取得し、SHA-256を検証します。第三者ライブラリの表示は[THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)、調査記録は[LEGAL_REVIEW.md](LEGAL_REVIEW.md)を確認してください。

`public/vendor/EBML.min.js`はts-ebml由来のMITライセンスソフトウェアです。著作権表示とMITライセンス本文は第三者ソフトウェア表示に収録し、配布物にも同梱します。

ロゴは道草45が制作し、著作権を保有するものとして扱います。ロゴおよび「道草45 Studio」の名称は、第三者ソフトウェアのライセンスによる再利用許諾の対象ではありません。

録音データの扱いは[PRIVACY.md](PRIVACY.md)、利用上の責任と免責は[TERMS.md](TERMS.md)を確認してください。

## 既知の制限

- Windows以外では動作確認していません。
- 録画終了後、AVI変換と保存が完了する前にアプリを終了すると保存されません。
- 自動保存には対応していません。
- 図形ロゴの商標類似調査は完了していません。
